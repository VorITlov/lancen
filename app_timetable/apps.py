from django.apps import AppConfig


class AppTimetableConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app_timetable'
    verbose_name = "Занятия и даты их проведения"
